# Git workflow

- Create a dedicated `codex/<feature-name>` branch before implementing every new feature requested by the user.
- Do not add a separate feature to an existing feature branch, even if that branch is clean.
- Keep fixes that directly belong to an unfinished feature on that feature's branch.
- Only commit, push, merge, or delete branches when the user explicitly asks for it.
